# CHANGELOG

## v1.0.7 Stable

### 修正

- Firestore Rules 改用 Map key membership 判斷，避免協作者不在 `serviceAccess.users` 時因不存在的 key 導致整段權限判斷失敗。
- 修正小幫手讀取所屬老師學生、服務紀錄、回收桶與操作紀錄的權限。
- 修正老師讀取與管理自己小幫手授權的規則。
- 操作紀錄依 `ownerEmail` 顯示整個個管團隊的操作。
- 小幫手無法透過直接切換畫面進入「系統設定」或「批次轉移」。
- 小幫手不再顯示學生還原按鈕，但仍可還原服務紀錄。
- 登入時增加載入畫面，避免權限尚未確認前顯示錯誤頁面。
- 補上 favicon，修正瀏覽器 404。

### 安全性

- 小幫手修改學生時不能改變 `ownerEmail`、建立者或刪除狀態。
- 修改服務紀錄時不能改變學生歸屬或原始建立者。
- 新增操作紀錄時必須寫入登入者所屬的個管團隊。

## v1.0.6

- 增加 `serviceAssistants` 授權來源。
- 小幫手隱藏系統設定。

## v1.0.5

- 操作紀錄改為個管團隊共用。
- 支援老師與系統管理員作為學生轉入對象。
